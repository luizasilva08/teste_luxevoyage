DB_HOST=mysql-luxevoyage-projetoluxevoyage.a.aivencloud.com
DB_PORT=18410
DB_USER=avnadmin
DB_PASSWORD=senha 123
DB_NAME=LuxeVoyage

EMAIL_REMETENTE=matheus_schneider150@estudante.sesisenai.org.br
EMAIL_DESTINATARIO=destinatario@empresa.com

# --- Autenticação (login do front-end) --------------------------------
# Chave usada para assinar os tokens JWT. Gere uma própria, por exemplo com:
#   python -c "import secrets; print(secrets.token_hex(32))"
# NUNCA reutilize o valor de exemplo abaixo em produção.
JWT_SECRET=troque-esta-chave-em-producao
JWT_EXPIRA_HORAS=24

# --- CORS ---------------------------------------------------------------
# Em desenvolvimento pode deixar em branco (libera tudo). Em produção,
# defina o(s) domínio(s) do front-end, separados por vírgula:
# ALLOWED_ORIGINS=https://ntask.vercel.app,https://www.luxevoyage.com.br
ALLOWED_ORIGINS=
